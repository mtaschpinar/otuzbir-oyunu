# Railway Environment Variables
# Bu değişkenleri Railway Dashboard > Variables bölümüne ekle

# Zorunlu: Rastgele güçlü bir string (örn: openssl rand -hex 32 ile üret)
JWT_SECRET=buraya-guclu-bir-secret-yaz

# Opsiyonel: NODE_ENV Railway tarafından otomatik ayarlanır
NODE_ENV=production
